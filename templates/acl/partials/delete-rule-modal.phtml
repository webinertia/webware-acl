<?php

declare(strict_types=1);

/**
 * Delete-rule confirmation modal fragment.
 *
 * Rendered by DeleteRuleModalHandler and swapped into
 * #sharedModalDialog (the shared modal shell in the layout).
 *
 * @var string $roleId
 * @var string $resourceId
 */
?>
<div class="modal-dialog modal-sm">
<div class="modal-content">
<div class="modal-header py-2 border-danger">
    <h5 class="modal-title small fw-bold text-danger" id="deleteRuleModalLabel">
        <i class="bi bi-trash3 me-1"></i> Delete Rule
    </h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>
<div class="modal-body small">
    Remove rule <code><?= $this->escapeHtml($roleId) ?></code> &rarr;
    <code><?= $this->escapeHtml($resourceId) ?></code>?
    <div class="text-secondary mt-1">This cannot be undone. Add a new rule if you need to restore it.</div>
</div>
<div class="modal-footer py-2">
    <button type="button" class="btn btn-sm btn-secondary" data-bs-dismiss="modal">Cancel</button>
    <button type="button"
            class="btn btn-sm btn-danger"
            hx-delete="<?= $this->escapeHtmlAttr($this->adminUrl('acl.manager.rule.delete', ['roleId' => $roleId, 'resourceId' => $resourceId])) ?>"
            hx-target="main"
            hx-push-url="false">Delete</button>
</div>
</div>
</div>
